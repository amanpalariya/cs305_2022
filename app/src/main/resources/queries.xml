<queries>
    <sql id="getMovie" paramType="java.lang.Integer">
     <![CDATA[
     SELECT title, description, length FROM film WHERE film_id=${value};
     ]]>
    </sql>
    <sql id="getMovies" paramType="java.util.ArrayList">
     <![CDATA[
     SELECT title, description, length FROM film WHERE film_id IN ${value};
     ]]>
    </sql>
    <sql id="updateMovie" paramType="java.lang.Integer">
     <![CDATA[
     UPDATE film SET title="NEW TITLE" WHERE film_id=${value};
     ]]>
    </sql>
    <sql id="insertActor" paramType="cs305.sqlrunner.MySqlRunnerTest$Actor">
     <![CDATA[
     INSERT INTO actor(first_name, last_name) VALUES(${firstName}, ${lastName});
     ]]>
    </sql>
    <sql id="deleteActor" paramType="java.lang.Integer">
     <![CDATA[
     DELETE FROM actor WHERE actor_id=${value}
     ]]>
    </sql>
    <sql id="findMovies" paramType="org.foo.Bar">
     <![CDATA[
     SELECT a, b, c FROM my_table WHERE x=${propX} AND y=${propY};
     ]]>
    </sql>
    <sql id="addMovie" paramType="org.foo.">
       <![CDATA[
       INSERT INTO my_table(x, y, x) VALUES(${propX}, ${propY}, ${propZ});
       ]]>
    </sql>
</queries>
